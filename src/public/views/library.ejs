<%- include('partials/header', { title: 'Library' }) %>
<div class="main-container" style="padding-top:var(--topbar-height);">
  <div class="main-col">
    <div class="main-section">
      <h2>Your Library</h2>
      <section id="libraryGrid" class="results-grid" aria-live="polite">
        <p style="grid-column:1/-1;font-size:var(--font-size-base);color:var(--color-text-dim);">No items yet.</p>
      </section>
      <!-- Full-view overlay (styled via shared theme.css) -->
      <div id="libraryOverlay" class="media-overlay" role="dialog" aria-modal="true" aria-label="Library media viewer">
        <button class="nav-btn prev-btn" type="button" aria-label="Previous">&#10094;</button>
        <div class="media-frame" id="libraryFrame">
          <button class="close-btn" type="button" aria-label="Close">✕</button>
        </div>
        <button class="nav-btn next-btn" type="button" aria-label="Next">&#10095;</button>
      </div>
    </div>
  </div>
</div>
<%- include('partials/navbar', { active: 'library' }) %>
